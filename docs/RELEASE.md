# Release

QuietGuard v1.4.0 is published through `.github/workflows/release.yml`.

The workflow validates and builds the release target, packages the Windows x64 executable with starter rule files, generates a SHA-256 sidecar, and creates or updates the GitHub Release for tag `v1.4.0`.

Release target commit: `f77b9a3bc8cf8ae43902b8880e42c3cc3f233d08`.
