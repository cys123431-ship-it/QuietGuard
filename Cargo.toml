[package]
name = "quietguard"
version = "1.5.1"
edition = "2021"
description = "Low-memory Windows PUP and system-change monitor"
license = "MIT"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
