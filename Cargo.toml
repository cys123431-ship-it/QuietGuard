[package]
name = "quietguard"
version = "1.4.0"
edition = "2021"
description = "Low-memory Windows PUP and system-change monitor"
license = "MIT"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
